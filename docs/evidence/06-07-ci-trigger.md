# 06 + 07 CI trigger

This file is a validation marker only. It exists to trigger the canonical `horus-ci` push workflow on the current API/Routing + Security source state. It contains no runtime logic and no production configuration.
